using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Transversal
{
    /// <summary>
    /// Created by the Interactor layer which creates it and passes it back
    /// to the delivery mechanism which then "knows" how to use it, transform it
    /// and pass it back to the user again
    /// </summary>
    public class OrderResultModel
    {
    }
}
